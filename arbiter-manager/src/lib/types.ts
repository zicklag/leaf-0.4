/**
 * Minimal shared types for the arbiter-manager.
 *
 * Most types come from `$lib/lexicons/town/muni/arbiter` — this file only
 * provides the handful of generic aliases that the UI layer needs.
 */

export type Did = string;