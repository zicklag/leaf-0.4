{
  "lexicon": 1,
  "id": "town.muni.arbiter.resolveMembers",
  "revision": 3,
  "description": "Internal server-to-server endpoint. Resolves and returns the full member list for a space, used when one arbiter delegates to a remote space on another arbiter. The calling arbiter authenticates via its own DID.",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["spaceKey"],
        "properties": {
          "spaceKey": {
            "type": "string",
            "description": "Key of the space on this arbiter to resolve."
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "ref",
          "ref": "town.muni.arbiter#resolvedMemberList"
        }
      },
      "errors": [
        { "name": "SpaceNotExists", "description": "The referenced space does not exist." }
      ]
    }
  }
}
