{
  "lexicon": 1,
  "id": "town.muni.arbiter.configureSpace",
  "revision": 3,
  "description": "Update the configuration of a permissioned space. The authenticated user must have ConfigureSpace access on the space.",
  "defs": {
    "main": {
      "type": "procedure",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["arbiterDid", "spaceKey", "publicRecords", "publicMembers"],
          "properties": {
            "arbiterDid": {
              "type": "string",
              "description": "DID of the arbiter managing the space."
            },
            "spaceKey": {
              "type": "string",
              "description": "Key of the space to configure."
            },
            "publicRecords": {
              "type": "boolean",
              "description": "Whether records in the space can be read by non-members."
            },
            "publicMembers": {
              "type": "boolean",
              "description": "Whether the member list can be queried by non-members."
            },
            "resolverDepth": {
              "type": "integer",
              "default": 3,
              "minimum": 0,
              "description": "Maximum depth for resolving remote space members. Defaults to 3."
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": [],
          "properties": {}
        }
      },
      "errors": [
        { "name": "ArbiterNotExists", "description": "No arbiter with this DID exists." },
        { "name": "SpaceNotExists", "description": "The referenced space does not exist." },
        { "name": "PermissionDenied", "description": "The user does not have ConfigureSpace access." },
        { "name": "PermissionChanged", "description": "The arbiter state changed since the request was submitted." }
      ]
    }
  }
}
