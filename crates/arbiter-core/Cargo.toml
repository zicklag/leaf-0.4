[package]
name = "arbiter-core"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.102"
im = { version = "15.1.0", features = ["serde"] }
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
quint-connect = "0.1.1"
